var arr = [1, 2, 3, 4, 5];
reverse = arr.reverse();
console.log(reverse); // Output: [5, 4, 3, 2, 1]

/*
In this code snippet, we are using the reverse() method to reverse the order of elements 
in an array.

1. We define an array 'arr' containing the numbers [1, 2, 3, 4, 5].
2. We call the reverse() method on the 'arr' array and store the result in the variable 'reverse'.
3. The reverse() method modifies the original array in place and returns a reference to the same array.
4. We log the 'reverse' variable to the console, which outputs the reversed array [5, 4, 3, 2, 1].

Overall, this demonstrates how to use the reverse() method to reverse the order of elements 
in an array in JavaScript.
*/